_target_: src.data.openbb_datamodule.OpenBBDataModule

# default parameters
sequence_length: 30        # Default sequence length
batch_size: 64             # Default batch size
cache_dir: "./data"       # Default cache directory
use_cache: true            # Default to using cache
provider: "fmp"  # Default data provider
openbb_output_type: "dataframe"  # Default output type


# dataframe settings
ticker: "AAPL"
train_date: ["2024-09-01", "2024-10-29"]  # Start and end date for training
val_date: ["2024-10-29", "2024-11-05"]    # Start and end date for validation
test_date: ["2024-11-05", "2024-11-07"]   # Start and end date for testing
interval: "1m"
market: "US"
